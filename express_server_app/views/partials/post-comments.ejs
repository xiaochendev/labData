<h2>Comments</h2>
<% if (comments.length > 0) { %>
  <ul>
    <% comments.forEach(comment => { %>
      <li>
        <%= comment.body %> — 
        <a href="/api/users/<%= comment.userId %>">
          <%= (users.find(u => u.id == comment.userId) || {}).username || 'Unknown' %>
        </a>
      </li>
    <% }) %>
  </ul>
<% } else { %>
  <p>No comments yet.</p>
<% } %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> - Post</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include('partials/nav') %>
    <%- include('partials/messages') %>
    <header>
        <h1><%= post.title %></h1>
    </header>

    <main>
        <h1><%= post.title %></h1>
        <p><%= post.content %></p>
        <% if (author) { %>
          <p><strong>Author:</strong> <a href="/api/users/<%= author.id %>"><%= author.name %></a></p>
        <% } else { %>
          <p><strong>Author:</strong> Unknown</p>
        <% } %>

        <%- include('partials/post-comments', { comments: postComments, users }) %>

        <h2>Edit Post</h2>
        <form action="/api/posts/<%= post.id %>?_method=PATCH" method="POST">
            <input name="title" value="<%= post.title %>">
            <textarea name="content"><%= post.content %></textarea>
            <button type="submit">Update</button>
        </form>

        <form action="/api/posts/<%= post.id %>?_method=DELETE" method="POST">
            <button type="submit">Delete Post</button>
        </form>
    </main>

</body>
</html>
